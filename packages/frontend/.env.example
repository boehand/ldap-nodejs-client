# LDAP UI Frontend Configuration

# API base URL (for production builds)
# In development, Vite proxy handles this automatically
VITE_API_BASE=/api

# Default LDAP URL (optional, shown as default in login)
VITE_DEFAULT_LDAP_URL=ldap://localhost:389

# Application title
VITE_APP_TITLE=LDAP UI
